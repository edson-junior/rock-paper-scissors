import React, { Component, Fragment } from 'react';

class Play extends Component {
  render() {
    return (
      <Fragment>
        <button>rock</button>
        <button>paper</button>
        <button>scissors</button>
      </Fragment>
    );
  }
}

export default Play;
